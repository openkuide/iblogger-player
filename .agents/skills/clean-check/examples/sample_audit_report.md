# Example: Clean Code Audit Report

This example demonstrates the standard format and structure of a clean code audit report generated by the `clean-check` skill.

---

## Clean Code Audit — assets/js/watch-progress.js

| # | Line | Rule (§ in guidelines) | Severity | Violation | Suggested fix |
|---|------|------------------------|----------|-----------|---------------|
| 1 | 42   | §2 Functions           | 🔴       | Function `saveMovieProgress` is 45 lines long, violating the size guideline (< 30 lines). | Extract local storage key building and progress parsing into separate monadic functions. |
| 2 | 87   | §6 Error Handling      | 🔴       | Try-catch block inside `resumeEpisode` is mixed with active retrieval and UI updates. | Isolate the error block into a dedicated error boundary handler and delegate bodies. |
| 3 | 114  | §1 Naming              | 🟡       | Variable name `dataObj` is a noise word. | Rename `dataObj` to `progress` to reveal intent. |
| 4 | 135  | §5 Comments            | 🔵       | Contains commented-out zombie code (`// console.log("saved", progress);`). | Delete the commented-out lines of code completely. |

**Score**: 6/10

### Top 3 wins:
1. **Isolate try-catch logic in `resumeEpisode`**: Improves error resiliency and code readability instantly.
2. **Decompose `saveMovieProgress`**: Splitting this long function will enforce the Single Level of Abstraction (SLA).
3. **Delete zombie code at line 135**: Cleans up formatting cruft in the repository.

*Run `/refactor assets/js/watch-progress.js` to fix these.*
