# Claude Code / local tooling — never publish to GitHub Pages
.remember/
CLAUDE.md
.claude/settings.local.json

# Source data export (input to build-db.py); db/ is the published output
movies-export-*.json

# OS / editor cruft
.DS_Store
Thumbs.db

*.swp

# Node / NPM dependencies
node_modules/

# Test debug screenshots (generated on failure by integration.test.js)
screenshots/
