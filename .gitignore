# Claude Code / local tooling — never publish to GitHub Pages
.claude/
.remember/
CLAUDE.md

# Source data export (input to build-db.py); db/ is the published output
movies-export-*.json

# OS / editor cruft
.DS_Store
Thumbs.db

*.swp

# Node / NPM dependencies
node_modules/
